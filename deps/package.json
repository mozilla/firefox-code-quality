{
  "name": "firefox-code-quality",
  "description": "REST API for retrieving dependencies graph for files in a Firefox codebase",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "3.x",
    "request": ">=2.0.0"
  }
}
